import 'package:flutter/material.dart';

/// Shared color palette for the HungerPoint Rider app — mirrors hpcustomer's
/// light theme so both apps feel like the same product.
class AppColors {
  AppColors._();

  static const Color amber = Color(0xFFFFC107);
  static const Color orange = Color(0xFFFF5722);
  static const Color background = Color(0xFFF9FAFB);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceBorder = Color(0xFFF3F4F6);
  static const Color appBar = Color(0xFFFFFFFF);

  static const Color textPrimary = Color(0xFF1E1B4B);
  static const Color textMuted = Color(0xFF6B7280);

  static const Color success = Color(0xFF2E7D32);
  static const Color danger = Color(0xFFEF4444);
}
